{
  "name": "planning-app",
  "version": "1.0.0",
  "description": "Time tracking application",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix react-timetracker\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix react-timetracker"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
